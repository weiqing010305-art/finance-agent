# FinScope Research — Prototype Design System

## Direction

“证据台账”把 Deep Research 呈现为一张可核查的研究案卷，而不是聊天机器人或指标仪表盘。报告是主工作面，来源是有编号的台账，研究进度是一条可观察的调查轨迹。

## Color

- `--paper`: `#F4F6F3`，工作区底色
- `--sheet`: `#FCFDFC`，报告正文
- `--ink`: `#17201D`，主文字
- `--muted`: `#64706B`，辅助文字
- `--line`: `#D9DFDB`，结构分隔
- `--pine`: `#145A46`，主要动作和选中态
- `--pine-soft`: `#DCEDE6`，可信证据与完成态
- `--amber`: `#9A5C12`，冲突与待核查

颜色策略为 restrained：中性工作面加单一松绿色，琥珀色只表示冲突。

## Typography

中文界面使用 `Inter, "PingFang SC", "Microsoft YaHei", system-ui` 工作字体。正文 14–15px，行高 1.7；研究标题使用 26–34px 的紧凑粗体。数字、时间和证券代码使用等宽数字特性，不把等宽字体当作装饰。

## Composition

- 密集信息由细分隔线、缩进、编号和留白组织，避免同尺寸卡片网格。
- 报告正文保持 65–75 字符阅读宽度。
- 来源编号同时出现在结论和证据台账，形成稳定的视觉索引。
- 研究任务的主动作始终靠近问题输入；首次研究与继续追问共用同一个自然语言输入框，不使用前置配置弹窗，也不单独展示研究计划进度行。
- Agent 选择位于输入框内部工具栏最左侧，采用可展开的紧凑胶囊；公司上下文紧随其后。左侧导航不再重复 Agent 选择，只承担新建研究与历史案卷。
- 继续追问是中间工作区的固定底部输入层，采用可容纳两行起步的多行编辑器；案卷正文在输入层上方独立滚动，不以悬浮遮挡内容。
- 顶部任务栏只保留研究对象、状态和问题，不设置调整、暂停或停止按钮；研究进行中时，输入框右侧主动作变为圆形停止按钮，任务结束后恢复为“继续研究”。
- Agent 工作轨迹位于案卷正文流顶部，随中间案卷一起滚动；标题采用“思考中/已思考（用时）”并支持展开收起，展开后按时间正序呈现检索、阅读、核验与写作动作，不占用固定网格行。
- 桌面端左右两侧是固定工具与证据上下文，只有中间案卷承担纵向阅读滚动；在两侧使用滚轮时也将滚动意图转交给中间案卷。

## Components and States

- 默认操作按钮高度为 40px，主要动作 42px，上下文操作最低 36px；输入框附近的胶囊按钮高度约 38px、字号 13px，形成接近主流 AI 对话产品的舒适点击尺度。
- 引用为 20px 圆形数字标记；悬停显示来源、机构、时间、摘录和访问动作。
- 搜索结果默认不常驻占宽，用户点击“查看搜索结果”后从右侧打开抽屉。抽屉不设置分类标签，只展示网站、标题和正文开头摘录；整条结果点击后在新标签页打开原始来源。
- 进度轨迹区分完成、进行中、等待和失败；不展示模型内部思维过程。
- 来源条目包含等级、发布日期、抓取状态；冲突以琥珀色文字与细线标记。
- 所有交互元素保留清晰键盘焦点，动画尊重 `prefers-reduced-motion`。

## Motion

唯一的主动画是“证据进入报告”：研究步骤推进时，来源条目轻微显现，最终报告解除遮罩。其他反馈只使用快速颜色和边框变化。

## Responsive

桌面保持报告与证据并置，左右固定、中间独立滚动；820px 以下恢复整页自然滚动，将证据台账放到报告下方，历史任务变为顶部横向列表。
